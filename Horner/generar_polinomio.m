#Esta función busca generar un polinomio a partir de inputs del usuario#No toma valores de entrada, pero devuelve el polinomio de grado y#coeficientes cargados manualmentefunction [vec] = generar_polinomio()  clear;  g = input('Grado del polinomio: ');  for i=1:g+1    if (i==g+1)      printf('Coeficiente independiente: ');    else      printf('Coeficiente del grado %i: ',g-i+1);    endif    vec(i) = input('');  endforendfunction